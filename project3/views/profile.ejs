<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Profile</title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <style>
    body {
      background: linear-gradient(135deg, #000000, #1c1c1c);
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      margin: 0;
    }

    .card {
      background-color: #121212;
      padding: 2rem 2.5rem;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(255, 255, 255, 0.05);
      text-align: center;
      max-width: 420px;
      width: 100%;
    }

    .avatar-wrapper {
      display: flex;
      justify-content: center;
    }

    .avatar {
      width: 130px;
      height: 130px;
      border-radius: 50%;
      object-fit: cover;
      border: 3px solid #0d6efd;
      margin-bottom: 1rem;
    }

    .btn-danger {
      padding: 0.6rem 1.5rem;
      font-size: 1rem;
    }

    h2, p {
      margin: 0;
    }

    p {
      margin-bottom: 1.5rem;
      color: #ccc;
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="avatar-wrapper">
      <img src="<%= user.imgurl %>" alt="Profile Image" class="avatar"/>
    </div>
    <h2 class="mt-3"><%= user.name %></h2>
    <p><%= user.email %></p>
    <a href="/logout" class="btn btn-danger mt-3">Logout</a>
  </div>
</body>
</html>
